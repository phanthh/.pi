{
  "name": "@pi-ext/cursor-auth",
  "version": "0.7.0-internal.0",
  "private": true,
  "description": "Pi extension providing Cursor OAuth and model access",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.10.2"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "catalog:",
    "@earendil-works/pi-coding-agent": "catalog:",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.1.3"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live": "node scripts/smoke-live.mjs",
    "typecheck": "tsc --noEmit",
    "check": "pnpm typecheck && pnpm test",
    "smoke": "node scripts/smoke-package.mjs",
    "refresh-models": "node scripts/refresh-models.mjs",
    "debug:timeline": "node scripts/debug-log-timeline.mjs"
  }
}
